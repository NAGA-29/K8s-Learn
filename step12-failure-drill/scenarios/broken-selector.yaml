apiVersion: v1
kind: Service
metadata:
  name: broken-api-svc
  namespace: mini-app
spec:
  # 意図的に壊れたselector（存在しないlabel値）
  selector:
    app: simple-api-typo  # 正しくは simple-api
  ports:
    - port: 8080
      targetPort: 8080
