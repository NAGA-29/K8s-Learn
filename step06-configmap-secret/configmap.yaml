apiVersion: v1
kind: ConfigMap
metadata:
  name: app-config
data:
  APP_ENV: "development"
  APP_LOG_LEVEL: "debug"
  config.json: |
    {
      "feature_flag": true,
      "max_connections": 100
    }
